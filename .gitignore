node_modules
.screenshots/
package-lock.json

# Per-machine Claude Code settings. `.claude/settings.json` is checked in and
# shared; the `.local.` one is where a machine-specific override goes, and this
# repo is public — without this line it would be committed like anything else.
.claude/settings.local.json

# Regenerated by a plugin hook on every SessionStart, in the clone and in every
# lane. It belongs to a work tool, not to this repo, and #22 found it reported
# as an uncommitted change by `gh pr create` at the moment you are deciding
# whether to merge.
.claude/commands/fixr.md
